# NS-Protocol web app

## Development

```shell
# install dependencies
pnpm install

# start dev server
pnpm start

# build for production
pnpm run build

# build for testing
pnpm run build --mode testing

# build for staging
pnpm run build --mode staging
```
