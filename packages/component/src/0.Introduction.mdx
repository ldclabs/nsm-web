import { Meta } from '@storybook/blocks'

<Meta title='Introduction' />

# NSM-Web component library

Learn to write a story in the [Storybook](https://storybook.js.org/docs/react/writing-stories/introduction) docs.

## Prerequisites

```typescript
// vite.config.ts
import svgr from '@svgr/rollup'
import { defineConfig } from 'vite'

export default defineConfig({
  plugins: [svgr({ ref: true })],
})
```

## Get started

```tsx
import { ThemeProvider, useTheme } from '@emotion/react'
import { GlobalStyles } from '@ldclabs/component'
import {
  Outlet,
  Route,
  RouterProvider,
  createBrowserRouter,
  createRoutesFromElements,
} from 'react-router-dom'
import Home from './routes/Home'

function Layout() {
  const theme = useTheme()

  return (
    <>
      <GlobalStyles />
      <Outlet />
    </>
  )
}

const router = createBrowserRouter(
  createRoutesFromElements(
    <Route element={<Layout />}>
      <Route path='/' element={<Home />} />
    </Route>
  )
)

export default function App() {
  return <RouterProvider router={router} />
}

function UserThemeProvider(props: React.PropsWithChildren) {
  return <ThemeProvider theme='light'>{props.children}</ThemeProvider>
}
```
